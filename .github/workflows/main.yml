on: [push]

jobs:
  zap_scan:
    runs-on: ubuntu-latest
    name: Scan the webapplication
    steps:
      - name: ZAP Scan
        uses: zaproxy/action-baseline@v0.2.0
        with: 
          token: ${{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}}
          docker_name: 'owasp/zap2docker-stable'
          target: 'https://www.demoblaze.com'
